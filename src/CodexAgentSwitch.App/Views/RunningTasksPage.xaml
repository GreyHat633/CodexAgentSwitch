<Page
    x:Class="CodexAgentSwitch.App.Views.RunningTasksPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CodexAgentSwitch.App.Views">
    <Grid Padding="28,24,28,20" RowSpacing="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <local:PageHeader
            Title="CodexAgentSwitch 模式"
            Subtitle="在本程序内按项目管理受控对话、主 Thread、Worker、预算、Usage 与归档。" />
        <InfoBar x:Name="ActionBar" Grid.Row="1" IsOpen="False" />

        <Grid x:Name="WorkspaceGrid" Grid.Row="2" ColumnSpacing="12" MinHeight="520">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="SidebarColumn" Width="280" />
                <ColumnDefinition Width="*" MinWidth="360" />
            </Grid.ColumnDefinitions>

            <Grid x:Name="SidebarGrid" Grid.Column="0" RowSpacing="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

            <Border Style="{StaticResource CardBorderStyle}" Padding="0">
                <Grid RowSpacing="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid Padding="12,12,8,4">
                        <TextBlock Text="项目" Style="{StaticResource SectionTitleStyle}" VerticalAlignment="Center" />
                        <Button Content="新建" HorizontalAlignment="Right" Tag="cas:project-new" AutomationProperties.Name="新建项目" />
                    </Grid>
                    <ListView
                        x:Name="ProjectListView"
                        Grid.Row="1"
                        MaxHeight="170"
                        SelectionChanged="OnProjectSelectionChanged"
                        AutomationProperties.Name="项目列表">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Padding="4" Spacing="3">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding WorkingDirectory}" Style="{StaticResource CaptionTextStyle}" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding State}" Style="{StaticResource CaptionTextStyle}" />
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <StackPanel Grid.Row="2" Padding="10" Spacing="6">
                        <Grid ColumnSpacing="6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Button x:Name="RenameProjectButton" Content="重命名" HorizontalAlignment="Stretch" Tag="cas:project-rename" IsEnabled="False" />
                            <Button x:Name="ProjectDirectoryButton" Grid.Column="1" Content="工作目录" HorizontalAlignment="Stretch" Tag="cas:project-directory" IsEnabled="False" />
                        </Grid>
                        <Grid ColumnSpacing="6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Button x:Name="ArchiveProjectButton" Content="归档" HorizontalAlignment="Stretch" Tag="cas:project-archive" IsEnabled="False" />
                            <Button x:Name="DeleteProjectButton" Grid.Column="1" Content="删除" Foreground="{ThemeResource SystemFillColorCriticalBrush}" HorizontalAlignment="Stretch" Tag="cas:project-delete" IsEnabled="False" />
                        </Grid>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Padding="0">
                <Grid RowSpacing="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid Padding="12,12,8,4">
                        <TextBlock Text="对话" Style="{StaticResource SectionTitleStyle}" VerticalAlignment="Center" />
                        <Button x:Name="NewConversationButton" Content="新建" HorizontalAlignment="Right" Tag="cas:conversation-new" AutomationProperties.Name="新建对话" IsEnabled="False" />
                    </Grid>
                    <TextBlock x:Name="EmptyConversationsText" Grid.Row="1" Text="当前项目还没有对话。" Margin="14" Style="{StaticResource CaptionTextStyle}" />
                    <ListView
                        x:Name="ConversationListView"
                        Grid.Row="1"
                        SelectionChanged="OnConversationSelectionChanged"
                        AutomationProperties.Name="对话列表">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Padding="4" Spacing="3">
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding Status}" Style="{StaticResource CaptionTextStyle}" />
                                    <TextBlock Text="{Binding Updated}" Style="{StaticResource CaptionTextStyle}" />
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <Grid Grid.Row="2" Padding="10" ColumnSpacing="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button x:Name="RenameConversationButton" Content="重命名" HorizontalAlignment="Stretch" Tag="cas:conversation-rename" IsEnabled="False" />
                        <Button x:Name="DeleteConversationButton" Grid.Column="1" Content="删除" Foreground="{ThemeResource SystemFillColorCriticalBrush}" HorizontalAlignment="Stretch" Tag="cas:conversation-delete" IsEnabled="False" />
                    </Grid>
                </Grid>
            </Border>
            </Grid>

            <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}" Padding="0" MinWidth="0">
                <Grid RowSpacing="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Padding="16,12" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Spacing="3">
                            <TextBlock x:Name="ConversationTitleText" Text="选择或新建对话" FontSize="20" FontWeight="SemiBold" />
                            <TextBlock x:Name="ConversationMetadataText" Text="一个对话始终绑定同一个主 Thread。" Style="{StaticResource CaptionTextStyle}" TextWrapping="Wrap" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                            <Button x:Name="ToggleSidebarButton" Content="隐藏侧栏" Tag="cas:toggle-sidebar" />
                            <Button x:Name="RetryButton" Content="重试" Tag="cas:retry" Visibility="Collapsed" />
                            <Button x:Name="StopButton" Content="停止生成" Tag="cas:stop" Visibility="Collapsed" Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
                        </StackPanel>
                    </Grid>

                    <ListView
                        x:Name="MessageListView"
                        Grid.Row="1"
                        Padding="14,8"
                        SelectionMode="None"
                        IsItemClickEnabled="False"
                        HorizontalContentAlignment="Stretch"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        AutomationProperties.Name="对话消息">
                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ItemsStackPanel Orientation="Vertical" AreStickyGroupHeadersEnabled="False" />
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <local:ConversationMessageView Message="{Binding}" HorizontalAlignment="Stretch" Margin="0,0,0,10" />
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <Border Grid.Row="2" Padding="14,12" BorderThickness="0,1,0,0" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <StackPanel Spacing="8">
                            <InfoBar x:Name="ConversationStatusBar" IsOpen="False" />
                            <TextBox
                                x:Name="ComposerTextBox"
                                MinHeight="82"
                                MaxHeight="220"
                                AcceptsReturn="True"
                                IsEnabled="False"
                                TextWrapping="Wrap"
                                PlaceholderText="输入下一轮内容；Ctrl+Enter 发送"
                                KeyDown="OnComposerKeyDown"
                                AutomationProperties.Name="对话输入框" />
                            <Grid x:Name="ComposerActionsGrid" ColumnSpacing="8" RowSpacing="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <StackPanel x:Name="WorkerControlsPanel" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                    <CheckBox x:Name="UseWorkerCheckBox" Content="允许当前方案调用 Worker" IsChecked="True" IsEnabled="False" />
                                    <Button x:Name="ForceWorkerButton" Content="强制测试当前 Worker" Tag="cas:force-worker" IsEnabled="False" />
                                </StackPanel>
                                <Button x:Name="ApprovalButton" Grid.Column="1" Content="处理审批" Tag="cas:approval" Visibility="Collapsed" />
                                <Button x:Name="SendButton" Grid.Column="2" Content="发送" Style="{StaticResource HeroButtonStyle}" Tag="cas:send" AutomationProperties.Name="发送对话" IsEnabled="False" />
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="CompactConversationLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="WorkerControlsPanel.Orientation" Value="Vertical" />
                        <Setter Target="WorkerControlsPanel.(Grid.ColumnSpan)" Value="3" />
                        <Setter Target="ApprovalButton.(Grid.Row)" Value="1" />
                        <Setter Target="ApprovalButton.(Grid.Column)" Value="1" />
                        <Setter Target="SendButton.(Grid.Row)" Value="1" />
                        <Setter Target="SendButton.(Grid.Column)" Value="2" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="WideConversationLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1280" />
                    </VisualState.StateTriggers>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>
