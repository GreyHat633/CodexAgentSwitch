<Page
    x:Class="CodexAgentSwitch.App.Views.RunningTasksPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CodexAgentSwitch.App.Views">
    <ScrollViewer>
        <StackPanel Padding="{StaticResource PagePadding}" Spacing="24" MaxWidth="1180">
            <local:PageHeader Title="运行任务" Subtitle="主代理拥有最终责任；工作代理只能在已登记边界内工作。" />
            <InfoBar x:Name="TaskActionBar" IsOpen="True" Severity="Informational" Title="当前没有正在运行的任务" Message="以下内容为界面预览使用的完整运行态示例。" />
            <Border Style="{StaticResource CardBorderStyle}" BorderBrush="{ThemeResource AccentFillColorDefaultBrush}">
                <StackPanel Spacing="16">
                    <Grid>
                        <StackPanel Spacing="4">
                            <TextBlock Text="主代理 · Sol（高）" Style="{StaticResource SectionTitleStyle}" />
                            <TextBlock Text="正在整合功能并执行最终审查" Style="{StaticResource CaptionTextStyle}" />
                        </StackPanel>
                        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Spacing="8">
                            <Border Style="{StaticResource StatusBadgeStyle}"><TextBlock x:Name="MainTaskStatusText" Text="运行中" /></Border>
                            <TextBlock Text="12:42" VerticalAlignment="Center" />
                        </StackPanel>
                    </Grid>
                    <ProgressBar x:Name="MainTaskProgress" IsIndeterminate="True" AutomationProperties.Name="主代理正在运行" />
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                        <Border Style="{StaticResource CompactCardBorderStyle}">
                            <StackPanel Spacing="8">
                                <TextBlock Text="计划跳过" Style="{StaticResource CaptionTextStyle}" />
                                <TextBlock Text="不再全量读取既有编排器或重建同一模块清单" Style="{StaticResource BodyTextStyle}" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="1" Style="{StaticResource CompactCardBorderStyle}">
                            <StackPanel Spacing="8">
                                <TextBlock Text="重复劳动" Style="{StaticResource CaptionTextStyle}" />
                                <TextBlock Text="未检测到完整重复" Style="{StaticResource BodyTextStyle}" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </Border>
            <Grid Margin="36,0,0,0" ColumnSpacing="16">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                <Border Style="{StaticResource CardBorderStyle}">
                    <StackPanel Spacing="12">
                        <Grid>
                            <TextBlock Text="工作代理 1 · Luna（中）" Style="{StaticResource SectionTitleStyle}" />
                            <Border HorizontalAlignment="Right" Style="{StaticResource StatusBadgeStyle}"><TextBlock Text="运行中" /></Border>
                        </Grid>
                        <TextBlock Text="审计现有 Sol–Luna 模块与当前协议" Style="{StaticResource BodyTextStyle}" />
                        <TextBlock Text="最近进展：正在核对应用服务器协议结构生成命令" Style="{StaticResource CaptionTextStyle}" />
                        <ProgressBar Value="62" AutomationProperties.Name="工作代理 1 估计进度 62%" />
                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock Text="用量" Style="{StaticResource CaptionTextStyle}" /><TextBlock Text="不可取得" /></StackPanel>
                            <StackPanel Grid.Column="1"><TextBlock Text="预算" Style="{StaticResource CaptionTextStyle}" /><TextBlock Text="原生额度" /></StackPanel>
                        </Grid>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button Content="查看详情" Tag="task:details" />
                            <Button Content="继续等待" Tag="task:continue" />
                            <Button Content="纠偏" Tag="task:refine" />
                            <Button Content="取消" Foreground="{ThemeResource DangerBrush}" Tag="task:cancel" />
                        </StackPanel>
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}">
                    <StackPanel Spacing="12">
                        <Grid>
                            <TextBlock Text="工作代理 2 · Terra" Style="{StaticResource SectionTitleStyle}" />
                            <Border HorizontalAlignment="Right" Style="{StaticResource StatusBadgeStyle}"><TextBlock Text="等待批准" Foreground="{ThemeResource WarningBrush}" /></Border>
                        </Grid>
                        <TextBlock Text="测试大文件扫描性能" Style="{StaticResource BodyTextStyle}" />
                        <InfoBar IsOpen="True" Severity="Warning" Title="需要批准" Message="工作代理请求读取任务范围外文件；默认不会自动批准。" />
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button Content="查看请求" Style="{StaticResource AccentButtonStyle}" Tag="task:approval-details" />
                            <Button Content="拒绝并纠偏" Tag="task:reject" />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
            <InfoBar IsOpen="True" Severity="Warning" Title="重复劳动提醒" Message="该范围已委派给工作代理 1。请选择等待、定向抽查、取消并接管，或记录本次允许重复的原因。" />
        </StackPanel>
    </ScrollViewer>
</Page>
