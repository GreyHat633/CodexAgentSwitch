<Page
    x:Class="CodexAgentSwitch.App.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CodexAgentSwitch.App.Views">
    <ScrollViewer>
        <StackPanel Padding="{StaticResource PagePadding}" Spacing="24" MaxWidth="1180" HorizontalAlignment="Stretch">
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <local:PageHeader
                    Title="早上好"
                    Subtitle="当前会话已准备好，所有高成本操作都会先经过预算与委派边界检查。" />
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <Button Content="停止 App Server" Click="StopCodex" AutomationProperties.Name="停止 Codex App Server" />
                    <Button x:Name="StartCodexButton" Content="启动 Codex" Click="StartCodex" Style="{StaticResource HeroButtonStyle}" AutomationProperties.Name="检测协议并启动受控 Codex App Server" />
                </StackPanel>
            </Grid>

            <InfoBar x:Name="DashboardActionBar" IsOpen="False" />

            <Border Style="{StaticResource CardBorderStyle}" Background="{ThemeResource AccentFillColorDefaultBrush}">
                <Grid ColumnSpacing="24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Spacing="8">
                        <TextBlock x:Name="ProfileNameText" Text="正在加载方案" FontSize="14" FontWeight="SemiBold" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
                        <TextBlock x:Name="AgentSummaryText" Text="正在读取本地配置" FontSize="28" FontWeight="SemiBold" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
                        <TextBlock x:Name="WorkerPolicyText" Text="预算与 Worker 边界正在加载" Foreground="{ThemeResource TextOnAccentFillColorSecondaryBrush}" />
                    </StackPanel>
                    <Button Grid.Column="1" Content="更改方案" VerticalAlignment="Center" Tag="navigate:profiles" />
                </Grid>
            </Border>

            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}">
                    <StackPanel Spacing="12">
                        <Grid>
                            <TextBlock Text="主代理" Style="{StaticResource SectionTitleStyle}" />
                            <Border HorizontalAlignment="Right" Style="{StaticResource StatusBadgeStyle}">
                                <TextBlock Text="已就绪" Foreground="{ThemeResource SuccessBrush}" />
                            </Border>
                        </Grid>
                        <TextBlock Text="Sol" FontSize="24" FontWeight="SemiBold" />
                        <TextBlock Text="推理强度 high" Style="{StaticResource CaptionTextStyle}" />
                        <TextBlock Text="负责复杂规划、集成与最终审查" Style="{StaticResource BodyTextStyle}" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}">
                    <StackPanel Spacing="12">
                        <Grid>
                            <TextBlock Text="Worker 策略" Style="{StaticResource SectionTitleStyle}" />
                            <Border HorizontalAlignment="Right" Style="{StaticResource StatusBadgeStyle}">
                                <TextBlock Text="原生" />
                            </Border>
                        </Grid>
                        <TextBlock Text="Luna ×1" FontSize="24" FontWeight="SemiBold" />
                        <TextBlock Text="优先明确、可独立验收的任务" Style="{StaticResource CaptionTextStyle}" />
                        <Button x:Name="DisableWorkerButton" Content="停用 Worker" HorizontalAlignment="Left" Tag="dashboard:disable-worker" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                    <StackPanel Spacing="12">
                        <Grid>
                            <TextBlock Text="预算" Style="{StaticResource SectionTitleStyle}" />
                            <TextBlock Text="安全" HorizontalAlignment="Right" Foreground="{ThemeResource SuccessBrush}" />
                        </Grid>
                        <TextBlock Text="¥0.00 / ¥3.00" FontSize="24" FontWeight="SemiBold" />
                        <ProgressBar Value="0" Maximum="3" AutomationProperties.Name="今日外部 API 预算使用 0%" />
                        <TextBlock Text="外部 API 当前未启用" Style="{StaticResource CaptionTextStyle}" />
                    </StackPanel>
                </Border>
            </Grid>

            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}">
                    <StackPanel Spacing="16">
                        <Grid>
                            <TextBlock Text="最近一次任务" Style="{StaticResource SectionTitleStyle}" />
                            <Button Content="查看报告" HorizontalAlignment="Right" Tag="navigate:history" />
                        </Grid>
                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Worker 结果" Style="{StaticResource CaptionTextStyle}" />
                                <TextBlock Text="部分采用" FontWeight="SemiBold" />
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="重复劳动" Style="{StaticResource CaptionTextStyle}" />
                                <TextBlock Text="低" FontWeight="SemiBold" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="经济判断" Style="{StaticResource CaptionTextStyle}" />
                                <TextBlock Text="可能节省" FontWeight="SemiBold" />
                            </StackPanel>
                        </Grid>
                        <InfoBar IsOpen="True" Severity="Informational" Title="诚实的用量结论" Message="没有纯 Sol 对照实验，因此不会显示伪精确的节省百分比。" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}" MinWidth="0">
                    <StackPanel Spacing="12">
                        <TextBlock Text="环境状态" Style="{StaticResource SectionTitleStyle}" />
                        <Grid ColumnSpacing="10" MinWidth="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <FontIcon x:Name="CodexStatusIcon" Glyph="&#xE73E;" FontSize="14" VerticalAlignment="Top" Margin="0,2,0,0" AutomationProperties.Name="Codex 状态图标" />
                            <TextBlock x:Name="CodexDetectedText" Grid.Column="1" Text="Codex 尚未检测" TextWrapping="Wrap" MinWidth="0" AutomationProperties.Name="Codex 状态文字" />
                        </Grid>
                        <Grid ColumnSpacing="10" MinWidth="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <FontIcon x:Name="AppServerStatusIcon" Glyph="&#xE73E;" FontSize="14" VerticalAlignment="Top" Margin="0,2,0,0" AutomationProperties.Name="App Server 状态图标" />
                            <TextBlock x:Name="AppServerStatusText" Grid.Column="1" Text="App Server 尚未启动" TextWrapping="Wrap" MinWidth="0" AutomationProperties.Name="App Server 状态文字" />
                        </Grid>
                        <Grid ColumnSpacing="10" MinWidth="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <FontIcon x:Name="CredentialStatusIcon" Glyph="&#xE73E;" FontSize="14" VerticalAlignment="Top" Margin="0,2,0,0" AutomationProperties.Name="凭据状态图标" />
                            <TextBlock x:Name="CredentialStatusText" Grid.Column="1" Text="凭据存储安全" TextWrapping="Wrap" MinWidth="0" AutomationProperties.Name="凭据状态文字" />
                        </Grid>
                        <Button Content="打开诊断" HorizontalAlignment="Left" Tag="navigate:diagnostics" />
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Page>
