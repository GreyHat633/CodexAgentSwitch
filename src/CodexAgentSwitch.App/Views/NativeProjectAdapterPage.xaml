<Page
    x:Class="CodexAgentSwitch.App.Views.NativeProjectAdapterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CodexAgentSwitch.App.Views">
    <Grid Padding="{StaticResource PagePadding}" MaxWidth="{StaticResource PageContentMaxWidth}" HorizontalAlignment="Center" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" MinHeight="0" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="8">
            <Button Content="返回工作模式" HorizontalAlignment="Left" Click="BackToDashboard" />
            <local:PageHeader
                x:Name="Header"
                Title="原生 Codex 项目配置"
                Subtitle="查看项目当前已应用的方案；只有明确勾选并确认后才会写入项目配置。" />
        </StackPanel>

        <InfoBar x:Name="ActionBar" Grid.Row="1" IsOpen="False" IsClosable="True" />

        <Grid x:Name="WorkspaceGrid" Grid.Row="2" ColumnSpacing="24" RowSpacing="16" MinHeight="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" MinWidth="0" />
                <ColumnDefinition Width="*" MinWidth="280" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" MinHeight="0" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border x:Name="ProjectCard" Style="{StaticResource CardBorderStyle}" Padding="0" MinWidth="0">
                <Grid RowSpacing="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" MinHeight="0" />
                    </Grid.RowDefinitions>
                    <StackPanel Padding="20,18,20,12" Spacing="12">
                        <Grid ColumnSpacing="10" RowSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" MinWidth="0" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <AutoSuggestBox
                                x:Name="ProjectSearchBox"
                                PlaceholderText="搜索项目名称或路径"
                                TextChanged="OnProjectSearchChanged"
                                AutomationProperties.Name="搜索适配项目" />
                            <Button Grid.Column="1" Content="添加现有文件夹" Click="AddExistingFolderAsync" />
                            <Button Grid.Column="2" Content="新建项目" Click="ToggleNewProjectPanel" />
                        </Grid>
                        <CheckBox x:Name="ShowArchivedCheckBox" Content="显示已归档项目" Checked="OnArchivedFilterChanged" Unchecked="OnArchivedFilterChanged" />
                    </StackPanel>

                    <Border x:Name="NewProjectPanel" Grid.Row="1" Visibility="Collapsed" Padding="20,0,20,16" BorderThickness="0,1,0,0" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <StackPanel Spacing="10">
                            <TextBlock Text="新建项目" Style="{StaticResource SectionTitleStyle}" />
                            <TextBox x:Name="NewProjectNameBox" Header="项目名称" PlaceholderText="例如：DarkGrey_RPG" TextChanged="OnNewProjectNameChanged" />
                            <Grid ColumnSpacing="10">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*" MinWidth="0" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                <TextBlock x:Name="NewProjectParentText" Text="请选择父目录" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" ToolTipService.ToolTip="新项目父目录" />
                                <Button Grid.Column="1" Content="选择父目录" Click="ChooseNewProjectParentAsync" />
                            </Grid>
                            <TextBlock x:Name="NewProjectPathText" Text="最终路径将在此显示" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" />
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                                <Button Content="取消" Click="ToggleNewProjectPanel" />
                                <Button Content="创建并选中" Style="{StaticResource HeroButtonStyle}" Click="CreateProjectAsync" />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Grid Grid.Row="2" Padding="12,0,12,12" MinHeight="0">
                        <TextBlock x:Name="EmptyProjectsText" Text="还没有项目。添加现有文件夹或创建一个新项目后，再勾选需要适配的目录。" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed" />
                        <ListView
                            x:Name="ProjectListView"
                            SelectionMode="None"
                            IsItemClickEnabled="True"
                            ItemClick="OnProjectItemClick"
                            HorizontalContentAlignment="Stretch"
                            ScrollViewer.VerticalScrollBarVisibility="Auto"
                            AutomationProperties.Name="原生 Codex 适配项目列表">
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate><ItemsStackPanel /></ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,0,8" Padding="16" CornerRadius="10" Background="{Binding CardBackground}" BorderBrush="{Binding CardBorder}" BorderThickness="1" MinWidth="0">
                                        <Grid ColumnSpacing="12" RowSpacing="6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" MinWidth="0" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <CheckBox IsChecked="{Binding IsSelected}" IsHitTestVisible="False" VerticalAlignment="Top" AutomationProperties.Name="选择项目" />
                                            <StackPanel Grid.Column="1" Spacing="4" MinWidth="0">
                                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                                <TextBlock Text="{Binding WorkingDirectory}" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{Binding WorkingDirectory}" Style="{StaticResource CaptionTextStyle}" />
                                                <TextBlock Text="{Binding NativeState}" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" />
                                                <TextBlock Text="{Binding ConfigurationPathText}" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{Binding ConfigurationPathText}" Style="{StaticResource CaptionTextStyle}" />
                                                <TextBlock Text="{Binding AppliedApplication}" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" />
                                                <TextBlock Text="{Binding PlannedApplication}" TextWrapping="Wrap" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" Style="{StaticResource CaptionTextStyle}" />
                                                <TextBlock Text="{Binding AppliedAtText}" Style="{StaticResource CaptionTextStyle}" />
                                            </StackPanel>
                                            <Button Grid.Column="2" Content="···" Click="OpenProjectMoreMenu" VerticalAlignment="Top" AutomationProperties.Name="项目更多操作" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Grid>
            </Border>

            <Border x:Name="SummaryCard" Grid.Column="1" Style="{StaticResource CardBorderStyle}" MinWidth="0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Spacing="16">
                        <TextBlock Text="待应用方案" Style="{StaticResource SectionTitleStyle}" />
                        <StackPanel Spacing="6">
                            <TextBlock x:Name="ProfileNameText" FontSize="22" FontWeight="SemiBold" TextWrapping="Wrap" />
                            <TextBlock x:Name="ProfileMainAgentText" TextWrapping="Wrap" />
                            <TextBlock x:Name="ProfileWorkerText" TextWrapping="Wrap" />
                            <TextBlock x:Name="ProfileRoutingText" TextWrapping="Wrap" />
                            <TextBlock x:Name="ProfileVersionText" Style="{StaticResource CaptionTextStyle}" />
                        </StackPanel>
                        <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,1,0,0" />
                        <TextBlock Text="已选择项目" Style="{StaticResource SectionTitleStyle}" />
                        <TextBlock x:Name="SelectedCountText" FontSize="24" FontWeight="SemiBold" />
                        <TextBlock x:Name="SelectedPathsText" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" />
                        <Border x:Name="ResultPanel" Visibility="Collapsed" Padding="12" Background="{ThemeResource ControlFillColorDefaultBrush}" CornerRadius="8">
                            <StackPanel Spacing="8">
                                <TextBlock x:Name="ResultSummaryText" FontWeight="SemiBold" TextWrapping="Wrap" />
                                <ItemsControl x:Name="ResultItemsControl">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <Border Grid.Row="3" Padding="16,12" Background="{ThemeResource CardBackgroundBrush}" BorderBrush="{ThemeResource CardBorderBrush}" BorderThickness="1" CornerRadius="12" Margin="0,8,0,0">
            <Grid ColumnSpacing="16" RowSpacing="8">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" MinWidth="0" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <TextBlock x:Name="FooterSelectionText" VerticalAlignment="Center" TextWrapping="Wrap" />
                <StackPanel x:Name="FooterActions" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                    <Button Content="取消" Click="BackToDashboard" />
                    <Button x:Name="ApplyButton" Content="应用配置并启动 Codex" Style="{StaticResource HeroButtonStyle}" Click="ConfirmApplyAsync" IsEnabled="False" AutomationProperties.Name="应用所选项目配置并启动官方 Codex" />
                </StackPanel>
            </Grid>
        </Border>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="WideNativeProjectLayout">
                    <VisualState.StateTriggers><AdaptiveTrigger MinWindowWidth="1120" /></VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="CompactNativeProjectLayout">
                    <VisualState.StateTriggers><AdaptiveTrigger MinWindowWidth="0" /></VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ProjectCard.(Grid.ColumnSpan)" Value="2" />
                        <Setter Target="SummaryCard.(Grid.Row)" Value="1" />
                        <Setter Target="SummaryCard.(Grid.Column)" Value="0" />
                        <Setter Target="SummaryCard.(Grid.ColumnSpan)" Value="2" />
                        <Setter Target="FooterActions.Orientation" Value="Vertical" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>
