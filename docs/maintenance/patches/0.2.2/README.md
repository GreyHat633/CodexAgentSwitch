# 0.2.2 migration patches

These patches were retained from the Luna worktree migration under
`E:\AISPace\主模型项目区\state\worktrees`.

The current `main` tree already contains the same file content as
`feat/0.2.2-external-runtime`, so these files are recovery/reference material,
not additional release inputs.

Original copies remain in the state directory. The state directory itself is
not required to build or run the main project.
